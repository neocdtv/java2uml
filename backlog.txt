COMMON:
Bootstrap:
multiple parameters -packageInputConfigs="/home/xix/Projects/java2uml/packageInputConfigs.json" -outputDir="/home/xix/Projects/java2uml"

for every packageName create a .dot
output:
    - create dir packages
    - generate packages.json: // be careful this name is also used by nmp
        [
          {
            "packageName" : "io.neocdtv.modelling.reverse.domain.customer"
            "relativePath" "output/packages/io.neocdtv.modelling.reverse.domain.customer.dot":
          },
          {
            "packageName" : "io.neocdtv.modelling.reverse.domain.offer"
             "relativePath" "output/packages/io.neocdtv.modelling.reverse.domain.offer.dot":
          }
        ]

replace PACKAGE_LINK with javascript link to app-context/relativePath

java2uml-ui:
create simple project, which will be able to deal with the generate packages.json

ECORE
- remove duplication from model
- check if the model is build correctly, how?
- ask about EEnums on eclipse forum, and how to add generalization

UML 2 ECORE
- check the conversion

Code Snippets:

- search in generated SVG for PACKAGE_LINK to replace it with a real link to the package
Array.from(document.getElementsByTagName('text'))
.forEach(function(text) { if (text.innerHTML.indexOf('PACKAGE_LINK') !== -1) { console.log(text.innerHTML); text.innerHTML = "<a href='#' onclick='alert(\"bla\")'>bla</a>"} });
  change bla to a real link inside the the fe-app